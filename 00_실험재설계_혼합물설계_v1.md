# 실험 재설계안 — 데이터 구성 혼합물설계와 구조 불변성 검정 (v1.3, 2026-07-15)

> v1.2→v1.3 (기여 층위 격상): "한 데이터셋에서 적합한 공식"은 그 자체로 기여가 아니라는 비판(정당함)을 수용 — 논문의 일반 주장을 **"공식의 구조 중 무엇이 데이터셋·탐지 객체를 넘어 불변이고 무엇이 도메인 의존적인가"** 로 격상. §5를 다중 데이터셋 구조 불변성 검정으로 전면 개편(D-Fire 전체설계 + IFireSmoke 변 부분설계 + 비화재 대조 셋), 구조의 조작적 정의와 판정 규칙 신설, 기존 C1~C4 교차 재현을 예비 증거로 명시. 단일 데이터셋 공식은 이제 '도구'이고 '발견'은 불변 구조다.
> v1.1→v1.2: 파일럿 학습 스케줄을 조기종료(100ep/patience30)에서 **고정 20에폭**으로 전환 — 실측 ETA(patience 방식 ~20~26h)가 데드라인과 충돌했고, 고정 에폭은 셀 간 유효 학습량(gradient step)까지 동일하게 만들어 §4.4-4의 위험을 원천 제거한다(실측 best 출현 10~15ep을 여유 포함하는 예산).
> v1→v1.1: 3중 적대 검토(통계학자·ML리뷰어·교수님 관점) 반영. 실내 전용 결정 동기화(§3.4·§3.6), 포화설계 오류 정정(§4.1~4.2), V검증 성공기준을 예측구간 기반으로 교체(§4.3), 알려진 위험·사전등록 해석규칙 신설(§4.4), 예상 반문 문답 신설(§11). **V1~V3 실측 전에 확정된 버전임(사전등록 지위).**

> 세미나 피드백("통제변수 명확화, 정량화, 구성 조합 체계화, 라벨 품질, 설계 우선") 대응 재설계.
> 실행 전략: Phase A 파일럿(N=6,000)을 즉시 착수해 설계 타당성과 1차 공식을 확보하고, 교수님 후속 피드백 시점에 "재설계 + 파일럿 실측"을 함께 제시한다. 작성: 총괄세션 / 실행: 분석세션 / 반영: 논문세션.

---

## 1. 재설계의 목표

기존 논문은 C1→C4 단계적 ablation으로 "데이터 구성이 중요하다"는 **관찰**을 보고했다. 재설계의 목표는 이를 한 단계 올려서, **화재 탐지 성능을 데이터 구성 비율의 함수로 예측하는 공식(반응표면)** 을 세우고 그 예측력을 검증하는 것이다.

- 관찰(기존): "균형을 맞추고 정상배경을 넣으니 좋아졌다"
- 공식(재설계): "화염 x₁, 연기 x₂, 정상배경 x₃ 비율이 주어지면 mAP를 이 식으로 예측할 수 있다"
- **일반 법칙(v1.3 격상, 논문의 최종 주장)**: "그 공식의 구조(계수의 부호, 우세 순위, 임계 거동) 중 무엇이 데이터셋과 탐지 객체를 넘어 **불변**이고, 무엇이 도메인 **의존**적인가" — 각 데이터셋의 공식은 측정 도구이고, 도구를 여러 도메인에 대어 얻는 불변/가변의 분리가 일반적 지식이다

세미나에서 지적된 문제와의 대응:

| 지적 | 대응 |
|------|------|
| 통제변수 불명확 (총량이 셀마다 다름) | 모든 셀에서 **학습 이미지 총량 N 완전 고정** (설계 원칙 §3.2) |
| 성능이 무엇에 영향받는지 정량화 부재 | Scheffé 다항식 적합 → 각 성분·상호작용의 기여가 **계수로 정량화** (§4) |
| 구성 조합의 체계성 부족 | 교수님이 제시한 7가지 구성(단독 3 + 2성분 3 + 3성분 1)이 통계학의 **simplex-centroid 혼합물설계**와 정확히 일치 — 이를 정식 프레임으로 채택 (§3.1) |
| "적용만 하고 비교만 한다" | 적합된 공식 자체가 본 연구의 산출물. **적합에 쓰지 않은 새 구성을 공식으로 먼저 예측하고 실측으로 검증**하는 전향적 검증 포함 (§5) |
| 라벨 품질(GIGO), 박스 크기 민감도 | 라벨 섭동 민감도 실험을 별도 축으로 추가 (§7) |

---

## 2. 연구 질문 (재편성)

- **RQ1**: 화재 탐지 성능(mAP@0.5, 클래스별 AP, 정밀도)은 학습 데이터의 성분 비율 (x_FL, x_SM, x_NM)의 함수로 어떤 형태를 갖는가? (반응표면 적합)
- **RQ2**: 적합된 공식은 학습에 사용하지 않은 새로운 구성의 성능을 예측하는가? (전향적 검증)
- **RQ3 (핵심 기여)**: 공식의 구조는 데이터셋(AIHub↔D-Fire↔IFireSmoke)과 탐지 객체(화재↔비화재)를 넘어 유지되는가 — **도메인 불변 요소와 도메인 의존 요소의 실증적 분리** (§5)
- **RQ4**: 공식은 라벨 품질 저하에 얼마나 강건한가? (라벨 섭동 민감도)

---

## 3. 핵심 설계 — 총량 고정 simplex-centroid 혼합물설계

### 3.1 설계점 (7점 + 검증점 3점)

세 성분의 비율 (x_FL, x_SM, x_NM), x_FL+x_SM+x_NM=1. 각 셀의 학습셋은 총 N장을 이 비율로 구성.

| # | 구성 | x_FL | x_SM | x_NM | 역할 |
|---|------|:---:|:---:|:---:|------|
| P1 | 화염 단독 | 1 | 0 | 0 | 정점 |
| P2 | 연기 단독 | 0 | 1 | 0 | 정점 |
| P3 | 정상 단독 | 0 | 0 | 1 | 정점 (성능 ≈ 0 예상, 경계 앵커) |
| P4 | 화염+연기 | 1/2 | 1/2 | 0 | 변 중점 |
| P5 | 화염+정상 | 1/2 | 0 | 1/2 | 변 중점 |
| P6 | 연기+정상 | 0 | 1/2 | 1/2 | 변 중점 |
| P7 | 화염+연기+정상 | 1/3 | 1/3 | 1/3 | 중심점 |
| V1 | 검증점 | 2/3 | 1/6 | 1/6 | **적합에 미사용**, 예측 검증 전용 |
| V2 | 검증점 | 1/6 | 2/3 | 1/6 | 〃 |
| V3 | 검증점 | 1/6 | 1/6 | 2/3 | 〃 |

- P1~P7이 Scheffé special cubic 모델(모수 7개)을 정확히 포화 적합하는 표준 simplex-centroid 배치.
- V1~V3(축점, axial points)은 적합에 쓰지 않고 남겨두었다가 RQ2 예측 검증에 사용. 표준 혼합물설계 문헌에서 모델 적합성 확인용으로 쓰는 위치와 동일.
- P3(정상 단독)은 탐지 대상이 전무하므로 mAP ≈ 0이 자명하나, 반응표면의 경계를 실측으로 고정하는 앵커로 포함(학습 비용 저렴). 교수님 제시 목록에도 포함되어 있음.

### 3.2 총량 N 결정 규칙 (통제변수 핵심) — 2단계 실행

- **모든 셀에서 학습 이미지 총량 N을 완전히 동일하게 고정한다.** 이것이 기존 설계와의 가장 큰 차이이며, "총량 증가 효과"와 "구성 효과"를 설계 단계에서 분리한다.
- 성분 풀은 **순수 단일라벨 이미지만** 사용한다 (동시라벨 이미지 제외 — §3.3).
- AIHub 순수 풀 실측(2026-07-06 카운트): 화염만 28,376 / 연기만 28,376 / 정상 28,695.
- ※ 화염만·연기만 풀이 정확히 같은 수(28,376)인 것은 우연치고 공교로우므로, 분석세션이 원본 라벨에서 독립 재카운트로 확인 후 N 확정.

**2단계 실행 (시간 제약 대응)**:

| 단계 | N | 목적 |
|------|:---:|------|
| **Phase A (파일럿)** | **6,000** | 설계 타당성 신속 확인 + 논문 초안용 1차 공식 적합. 회당 학습이 짧아 7점×3시드가 하룻밤 내 완료 가능 |
| **Phase B (본실험)** | **28,374** (풀 최대) | 파일럿에서 설계가 작동함을 확인한 뒤 최종 수치 확정. 초안의 수치를 교체 |

- 파일럿 N=6,000 근거: 구성 효과의 방향·순서는 소규모에서도 안정적으로 재현됨이 이미 실측 확인되어 있다 — D-Fire 셀들(3,828~11,056장)에서 멀티시드 std ±0.005 수준으로 C1<C2<C3<C4 순서가 견고했고, 동일 총량 통제(E09/E10, 7,664장)에서도 +1.4%p 차이가 검출되었다. 6,000은 이 검증된 범위 안이며 1/2·1/3·1/6 분할이 모두 정수가 된다(3,000/2,000/1,000).
- 파일럿의 한계(정직 고지): 절대 성능은 N에 의존하므로 파일럿 계수는 잠정치다. 논문 초안에는 파일럿 수치로 공식·그림을 구성하되 "N=28K 본실험으로 확정 예정"을 명시하고, Phase B 완료 시 수치만 교체한다. 공식의 **구조**(어느 항이 지배하는지)가 두 N에서 일치하는지 자체가 "규모 불변성" 추가 근거가 된다.

### 3.3 동시라벨 이미지 처리 원칙

AIHub는 화염·연기 동시라벨 이미지가 29,015장(NM 제외 시 33.8%) 존재한다. 본 설계에서는 이를 **전량 제외**한다.

- 이유: 동시라벨 이미지를 단일 성분으로 쓰려면 한쪽 라벨을 삭제해야 하는데, 이는 화면에 존재하는 객체를 라벨에서 지우는 **인위적 라벨 노이즈**를 만들어 GIGO 지적을 스스로 재현하는 것임.
- 트레이드오프(정직 고지): 동시라벨 데이터(29,015장)가 순수 풀보다 오히려 많으며, 실제 화재 장면의 상당수는 화염·연기 동시 발생임. 따라서 본 설계는 "성분 효과의 순수 분리"를 위한 통제 실험이고, **기존 C1~C4(동시라벨 포함 자연 구성) 결과를 현실 조건 대조군으로 병기**하여 상호 보완한다.

### 3.4 표본 선별 규칙 (실내 전용 + 클립 단위 무작위 추출)

**실내 전용(indoor-only) 결정 (2026-07-14 실측 후 확정)**: 순수 화염 풀의 97.3%가 실내(실외 763장뿐)로 확인되어, 실내/실외 층화는 실외 화염 763장이 전 시드에서 전량 뽑혀야 하는 형식적 층화가 된다. 층화를 포기하면 셀 간 도메인 구성이 달라져 §3.4가 차단하려던 교란이 재현된다. 따라서 **전 셀 100% 실내로 고정**한다 — 도메인이 상수가 되어 순수 성분 효과가 분리되고, 본 연구의 실내 배포(건축물 비화재보) 스코프와도 정합한다. 실외 일반화는 별도 축(§5 D-Fire 재현)으로 다룬다.

1. **클립 단위 처리**: 같은 원본 클립의 프레임들이 특정 셀에 몰리지 않도록 클립 단위로 추출 (근접중복 편중 방지, held-out test 때와 동일 원칙).
2. **무작위성**: 시드 기반 무작위 추출. 모든 셀이 동일 유한 풀을 공유하고 같은 시드를 재사용하므로 셀 간 관측은 독립이 아니다 — 이는 결함이 아니라 **공통난수(common random numbers) 블록 설계**로, 셀 간 대비의 분산을 줄이는 표준 기법이다. 분석 시 시드를 블록 요인으로 취급한다(§4.2).

### 3.5 시드 규칙

- 시드 하나가 **표본 추출과 학습 초기화를 동시에 결정** (seed=0,1,2 각각 표본도 다르고 초기화도 다름).
- 셀당 n=3 반복 → 보고값은 mean±std. 이 std는 표본추출 분산과 학습 분산을 합친 값으로, 공식의 예측 오차 기준선이 된다.
- 표본추출 분산과 학습 분산의 완전 분리는 (표본 M)×(시드 K) 요인 설계가 필요해 비용이 곱으로 늘어나므로 본 설계에서는 하지 않고 한계로 명시.

### 3.6 고정 조건 (통제변수 목록)

| 변수 | 고정값 |
|------|--------|
| 학습 총량 N | Phase A 6,000 / Phase B 28,374 (각 단계 내 전 셀 동일) |
| 모델 | YOLO11n (COCO 사전학습) |
| 하이퍼파라미터 | 기존 실험과 동일 (AdamW lr0 0.001, batch 64, 증강 동일) — 단 학습 스케줄은 **고정 20에폭, patience 비활성**(Phase A, v1.2 변경). 체크포인트는 20ep 내 모니터 subset 기준 best.pt, 전 셀 동일 규칙. 기존 관례(100ep/pat30)와 다른 점은 전 셀 동일 예산이므로 셀 간 비교에 유효 |
| 평가셋(주 반응변수) | **실내 val 10,512 고정** (실내 전용 학습과 정합) — 전체 val 19,080·held-out 7,632는 참고 병기 |
| 조기종료 모니터링 | 실내 val에서 클립 단위로 1회 추출한 2,000장 고정 subset (전 셀·전 시드 공통 — 셀당 19,080장 매 에폭 검증의 병목 제거, 최종 지표는 위 평가셋에서 별도 산출) |
| 해상도 | 640 |

---

## 4. 공식 (본 연구의 산출물)

### 4.1 모델 형태 — Scheffé special cubic

각 반응변수 y ∈ {mAP@0.5, fire AP, smoke AP, Precision}에 대해:

$$y(x) = \beta_1 x_{FL} + \beta_2 x_{SM} + \beta_3 x_{NM} + \beta_{12} x_{FL}x_{SM} + \beta_{13} x_{FL}x_{NM} + \beta_{23} x_{SM}x_{NM} + \beta_{123} x_{FL}x_{SM}x_{NM}$$

- 1차항 βᵢ: 각 성분 단독의 기여 (정점 성능과 일치)
- 2차 교호항 βᵢⱼ: 두 성분을 섞을 때의 **시너지/길항** — 예: β₁₂ > 0이면 화염·연기를 함께 학습하는 것이 각각의 가중평균보다 낫다는 뜻. 이 계수들이 "균형이 왜 효과적인가"의 정량적 답
- 3차항 β₁₂₃: 세 성분 모두 섞였을 때의 추가 효과 — **정상배경(NM)의 기여가 어느 항에 실리는지**가 비화재보 저감 주장의 정량 근거가 됨
- n=3 반복으로 순수오차는 추정 가능하나, **7점=7모수 포화 적합이므로 적합결여(lack-of-fit) 자유도는 0**이다 — 모델은 셀 평균을 정확히 통과한다. 따라서 모델 적합성 진단은 설계 내부가 아니라 **V1~V3 축점이 담당**한다(축점이 전향적 예측 검증과 적합성 진단을 겸함 — 표준 혼합물설계에서 축점의 정확한 용도).

### 4.2 적합·진단 프로토콜 (사전 등록)

1. P1~P7 (각 n=3, 총 21개 관측)으로 적합. **가중치는 원시 셀분산이 아닌 합동(pooled) 분산 기반** — P3(정상 단독)은 셀분산이 0에 수렴해 원시 WLS는 가중치가 발산하므로 금지. 주 분석은 OLS + 시드 블록 요인(공통난수 설계, §3.4) + 로버스트 SE.
2. 교차검증은 leave-one-**observation**-out (21개 관측 단위 — 항상 식별 가능. leave-one-point-out은 6점으로 7모수를 추정하는 특이 문제라 불가).
3. 민감도 분석 (사전 등록): ① logit 변환 적합 병행 (mAP가 [0,1] 유계이므로 경계 근처 예측이 범위를 벗어나는지 진단), ② P3 제외 6모수 적합 (퇴화 정점이 표면을 왜곡하는지 확인), ③ (v1.2에서 불필요해짐 — 고정 에폭 전환으로 전 셀 학습량 동일, §4.4-4).
4. 반응변수별로 별도 적합하되 **클래스별 AP 표면(fire AP, smoke AP)을 주 해석 축으로 승격** — mAP는 두 AP의 평균 구조라 x_SM=0 경계에서 smoke AP가 정의상 0으로 붕괴하는 구조적 절벽을 물려받는다. 클래스별 표면이 더 해석적이고, mAP 표면은 종합 참고로 병기.

### 4.3 예측 검증 (RQ2) — 사전 등록 기준 (v1.1 확정판)

- V1~V3을 **적합 완료 후에** 학습·실측. 예측값·구간을 실측 전에 문서로 고정.
- **주 성공 기준**: 각 V점의 3시드 평균 실측이 적합 모형의 **t분포 기반 95% 예측구간** 내에 있을 것. 예측분산 = (계수 불확실성 x'Σx) + (순수오차 s²/3, 3시드 평균이므로), 자유도 = 잔차 자유도(14). 이 기준은 계수 추정 불확실성과 관측 잡음을 모두 반영한다.
- **다중성 정직 고지**: 3점이 각각 95%면 동시 통과의 명목 결합피복률은 약 86%(0.95³) — 옳은 모델도 ~14% 확률로 한 점 이상 벗어날 수 있음을 명시하고, 판정은 "3점 중 몇 점이 구간 내인가 + 벗어난 점의 방향·크기"를 함께 보고한다.
- **보조 기준(해석용)**: |예측 − 실측| ≤ 2×(P셀 시드 std 중앙값) — 실무적 오차 크기의 직관 제공용.
- V점 이탈 시의 해석 규칙(사전 등록): 이탈은 실패가 아니라 **함수형 진단**으로 해석한다 — 특히 저비율 지점(x=1/6)에서의 체계적 이탈은 소수 클래스 급포화(기존 C2 실측: 연기 6.7%만으로 smoke AP 0.616)를 special cubic이 근사하지 못함을 시사하며, Phase B에서 simplex-lattice {3,2} 확장(변당 저비율 점 추가) 또는 포화형 기저(로그 비율 등)로의 개선 방향을 제시하는 근거가 된다.

---

### 4.4 알려진 위험과 사전 등록 해석 규칙 (3중 적대 검토 반영, 2026-07-14)

1. **증강이 성분을 섞는 문제 (mosaic 1.0)**: 이미지 목록 수준에서 성분을 분리해도 mosaic이 배치 내 4장을 합성하므로 P4는 화염·연기 공존 합성 장면으로 학습된다. 본 공식은 **"표준 학습 파이프라인(증강 포함) 조건부 구성 법칙"**으로 정의한다 — 증강이 전 셀 동일하므로 셀 간 비교는 유효하다. 강건성 확인으로 Phase B에서 P4 1시드 mosaic=0 재학습을 계획(부록행).
2. **비율 매개변수화의 한계**: 학습은 절대 장수에도 의존하므로(N=6,000의 x_SM=1/6은 연기 1,000장, N=28,374에선 4,729장), 본 공식은 **고정 예산 N 조건부 법칙(fixed-budget composition law)**이다 — 고정 연산량 슬라이스로 제시되는 스케일링 법칙과 동일한 관례. Phase A↔B 계수 구조 비교는 "규모 확인"이 아니라 **비율 불변성의 실증 검정**으로 격상한다: 일치하면 강한 법칙 주장, 불일치하면 절대량 문턱 효과의 발견으로 보고(양쪽 모두 학술적 가치).
3. **train/eval 분포 비대칭**: 학습은 동시라벨 배제, 평가(실내 val)는 동시라벨 ~1/3 포함. 평가셋이 전 셀 공통이므로 **셀 간 비교는 오염되지 않으나**, 절대 성능에는 "동시장면 일반화 격차"가 포함된다. 최종 평가에서 실내 val을 단일라벨/동시라벨 층으로 나눈 층화 지표를 병기한다(재평가만, 저비용) — 공식이 두 층에서 모두 성립하면 방어 완성, 층간 차이가 크면 그 자체가 분석 절이 된다.
4. **유효 학습량 차이 (v1.2에서 설계 수준 해결)**: 조기종료는 셀별 종료 에폭이 달라 gradient step 수가 다른 문제가 있었다 — Phase A는 **전 셀 고정 20에폭**으로 전환해 이 위험을 원천 제거했다(총 이미지 수와 학습 노출량이 모두 동일). Phase B도 동일하게 고정 에폭(수렴 여유를 반영해 재산정)으로 진행한다.
5. **Precision의 작동점 문제**: Ultralytics 기본 P/R은 런마다 F1 최대화 임계값에서 보고되어 셀 간 작동점이 다르다. Precision 표면은 **고정 신뢰도(conf=0.25)에서 재산출한 값**만 사용하거나 임계값 비의존인 AP 계열로 한정한다(저장된 PR 곡선에서 후처리 추출, 재학습 불필요).
6. **순수 풀의 조작적 정의**: "화염만 장면"은 특정 장면 유형(초기 단계·단순 구도 개연)이므로, 성분은 추상적 데이터 유형이 아니라 **"단일라벨 장면 유형"으로 조작적으로 정의**됨을 명시한다. 이 정의가 데이터셋 특수적이라는 공격에는 D-Fire 구조 전이 검정(§5)이 답한다.

## 5. 구조 불변성 검정 (RQ3) — 논문의 핵심 기여 (v1.3 격상)

### 5.1 구조의 조작적 정의

"구조가 유지된다"를 다음 세 층으로 사전 정의한다 (계수 절대값 비교는 하지 않음 — 도메인 의존이 당연하므로):

1. **부호 패턴**: 각 계수(특히 교호항 β₁₂, β₁₃, β₂₃)의 부호가 데이터셋 간 일치하는가 — 예: "화염·연기 공동학습 시너지(β₁₂>0)"는 보편인가
2. **우세 순위**: 반응 범위로 표준화한 계수의 크기 순위가 일치하는가 — 예: "균형 시너지 ≫ NM 기여"라는 서열이 보편인가
3. **임계 거동**: 저비율 지점(V점)의 이탈 방향이 일치하는가 — 소수 클래스 급포화가 보편적 형상인가

### 5.2 검정 대상 (도메인 사다리)

| 데이터셋 | 설계 | 검정하는 일반화 축 |
|----------|------|------------------|
| AIHub 실내 (주) | 7셀 전체 + V1~V3 | 기준 공식 |
| D-Fire | 7셀 전체 (순수 풀 카운트 후 N_DFire 확정, 수천 장 예상) | 같은 객체, 다른 데이터셋 |
| IFireSmoke | **FL-SM 변 부분설계만** (P1·P2·P4 + 변 위 저비율점) — NM 이미지가 0장이라 3성분 설계 불가, 시너지 항(β₁₂) 구조만 검정 | 같은 객체, 제3 데이터셋 |
| 비화재 공개 셋 1개 (2클래스+배경, 후보 선정은 §12) | 7셀 전체 | **탐지 객체를 바꿔도** 성립하는가 |

### 5.3 예비 증거 (이미 확보된 교차 재현)

구조 불변성 가설은 백지 가설이 아니다 — 기존 C1~C4 실험에서 이미:
- 구성 순서 C1<C2<C3<C4가 AIHub·D-Fire **양쪽에서 재현** (계수 부호 패턴 불변의 전조)
- NM 효과: **방향은 양쪽 모두 양(+), 크기는 데이터셋 의존**(+0.6%p vs +5.5%p) — "부호 불변, 크기 가변"이라는 v1.3 주장의 원형
- 모델 용량 포화: 양쪽 모두 11m 이상 무이득 — 구성 우세 서열의 불변 전조

### 5.4 판정 규칙 (사전 등록)

- 부호 패턴·우세 순위가 화재 3셋 + 비화재 1셋에서 유지 → **"탐지 일반의 구성 법칙 구조"** 주장 (강한 기여)
- 화재 셋 내에서만 유지 → **"화재 도메인 구성 법칙"** 주장 (한정 기여, 여전히 유효)
- 화재 셋 내에서도 깨짐 → **도메인 특이성의 정량화**로 보고 (구성 지침이 데이터셋마다 달라야 함을 실증 — 실무적으로 오히려 중요한 발견)
- 어느 경우든 결과를 은폐하지 않고 해당 층위로 주장을 조정한다.

---

## 6. NM 포화 곡선 (선택 확장)

기존 관찰("NM은 약 3,000장에서 포화")을 공식화: x_FL=x_SM 고정선 위에서 x_NM만 변화시키는 추가 점 2~3개로 정밀도-NM 곡선을 적합. 포화형 함수(예: 지수 포화)의 시정수가 "비화재보 억제에 필요한 최소 정상배경량"이라는 실무 공식이 됨. 우선순위는 RQ1·2보다 낮음 — 여력 시 수행.

---

## 7. 라벨 품질 축 (RQ4) — GIGO·박스 크기 지적 대응

Gaussian YOLO 등 라벨 불확실성을 모델 측에서 다루는 접근은 **관련연구로 인용만** 하고 채택하지 않는다 (탐지기를 바꾸면 구성 효과와 모델 효과가 다시 교란되어 본 설계의 통제 원칙에 위배). 대신 데이터 측 통제 실험으로 답한다:

- **평가측 민감도 (재학습 없음, 저비용)**: 고정 val의 GT 박스를 체계적으로 축소/확대(±10%, ±20%)하며 동일 예측에 대한 AP 변화를 측정 → "박스 크기 판단이 사람마다 달라 AP가 달라진다"는 지적을 정량화.
- **학습측 민감도 (선택, 소수 셀만)**: 중심점(P7) 구성에서 학습 GT에 박스 크기/위치 섭동을 주입해 재학습 → 라벨 노이즈가 공식의 계수를 얼마나 흔드는지 확인.

---

## 8. 기존 자산의 재배치 (엎어지는 것과 살아남는 것)

| 기존 자산 | 재설계 후 위치 |
|-----------|---------------|
| C1~C4 ablation (양 데이터셋) | 예비 실험으로 격하 — "왜 비율 기반 정밀 설계가 필요한가"의 동기 |
| 증분 R² 분해모델 (2026-07-14) | 예비 분석 — 기존 데이터의 한계(공선성) 명시가 오히려 재설계 정당화 |
| 실내/실외 분리·클래스통제·held-out | 유지 — 평가 프로토콜의 강건성 근거로 그대로 사용 |
| 도메인 전이·결합학습 | 축소 유지 또는 부록 |
| Jetson 엣지 배포 | 유지 — 응용 절 |
| IFireSmoke 직접비교 | 유지 — 외부 벤치마크 참고 |
| E09/E10 총량통제 | 유지 — 새 설계와 같은 철학의 선행 통제 실험으로 인용 |

---

## 9. 실행 규모 추정 (2단계)

**Phase A — 파일럿 (N=6,000, 최우선·즉시 착수)**

| 단계 | 학습 횟수 | 비고 |
|------|:---:|------|
| 풀 재카운트·추출 스크립트 | 0 | 전처리 (N과 무관하게 1회) |
| P1~P7 × 3시드 | 21 | 셀당 6K장 × 고정 20ep(실측 ~103초/ep) ≈ 회당 ~35분 → 총 ~12h |
| 1차 공식 적합 + V1~V3 × 3시드 | 9 | 적합 이후 |
| **Phase A 합계** | **30** | |

**Phase B — 본실험 (N=28,374, 파일럿 타당성 확인 후)**

| 단계 | 학습 횟수 | 비고 |
|------|:---:|------|
| P1~P7 + V1~V3 × 3시드 | 30 | 셀당 28K — 기존 114K 학습의 약 1/4 비용 |
| D-Fire 재현 (P1~P7 × 3시드) | 21 | 셀당 수천 장 — 저비용 |
| 라벨 섭동 (평가측) | 0 | 재평가만 |
| 라벨 섭동 (학습측, 선택) | 3~6 | P7만 |
| **Phase B 합계** | **51~57** |

**Phase C — 구조 불변성 검정 (v1.3 신설, Phase B와 병행 가능)**

| 단계 | 학습 횟수 | 비고 |
|------|:---:|------|
| D-Fire 7셀 × 3시드 | 21 | Phase B 표의 "D-Fire 재현"을 흡수 (셀당 수천 장, 저비용) |
| IFireSmoke FL-SM 변 부분설계 (3~5점 × 3시드) | 9~15 | 5,000장 소규모라 초저비용 |
| 비화재 대조 셋 7셀 × 3시드 | 21 | 데이터셋 선정·전처리 선행 (§12) |
| **Phase C 합계** | **51~57** | 구조 판정(§5.4)이 논문의 최종 주장 층위를 결정 | |

---

## 10. 선행연구 조사 결과 (2026-07-14 완료, WebSearch 3건 병렬 수행)

### 10.1 Novelty 판정 (정직 버전)

| 주장 | 판정 | 근거 |
|------|:---:|------|
| 혼합물설계+Scheffé를 **객체 탐지** 데이터 구성에 적용한 최초 | **가능** | 유일한 근접 선행은 Bagad & Zisserman(2025, 비디오-텍스트 검색, 부록 ablation 수준) — 탐지 아님 |
| **화재/연기 탐지**에서 고정 N + 3성분 비율 체계 변화 최초 | **가능** | 직접 선행 없음. 근접: Grimmer(2025, 부유쓰레기, 음성비율 5단계·2성분·N 비고정), Jin(2022, 위암, 양성:음성 계열·N 비고정) |
| "ML 학습데이터 구성에 혼합물설계 최초 적용" (광의) | **불가** | Bagad & Zisserman(2025)이 고정 20K 예산 + Scheffé 반응표면 선사용 |
| 평가측 GT 박스 섭동으로 AP 측정 불확실성 대역 산출 | **가능(소규모)** | Borji(2022)는 예측 박스 중심 — GT 측으로 뒤집는 것은 직접 선행 없음 |

### 10.2 서론·관련연구 인용 목록 (핵심만)

**데이터 혼합→성능 공식화 (문제의식의 정당성)**
- Ye et al., "Data Mixing Laws," ICLR 2025 (arXiv:2403.16952) — LLM 혼합비→손실 예측식
- Xie et al., "DoReMi," NeurIPS 2023 (arXiv:2305.10429) / Liu et al., "RegMix," ICLR 2025 (arXiv:2407.01492)
- Bagad & Zisserman, arXiv:2512.13511 (2025) — 고정 예산 혼합물설계+Scheffé (검색 도메인, 최근접 선행)

**클래스 비율·배경 비율 (간접 근거)**
- Oksuz et al., "Imbalance Problems in Object Detection: A Review," IEEE TPAMI 2021 — 표준 서베이
- Gupta et al., "LVIS," CVPR 2019 — repeat factor sampling (구성비→클래스별 AP 직결의 근거)
- **Ultralytics 배경이미지 0~10% 권고 — 체계적 실험 근거가 제시된 적 없는 경험칙** → "널리 쓰이는 권고에 실험적 근거가 없다"가 본 연구의 동기 문장으로 사용 가능 (강력한 훅)
- Grimmer et al., 2025 (arXiv:2510.23798) — 음성비율 0~40% 변화, 20% 최적 (타 도메인, 2성분)
- D-Fire (de Venâncio 2022) — 화재유사 음성 포함 설계는 있으나 비율 실험 없음

**라벨 품질 (GIGO 지적 대응)**
- Borji, "Sensitivity of AP to Bounding Box Perturbations," 2022 (arXiv:2206.10107) — 1px 이동에 mAP −8.4%
- Tschirschwitz & Rodehorst, "Label Convergence," WACV 2025 — 라벨 모순에 의한 mAP 상한 존재
- Choi et al., "Gaussian YOLOv3," ICCV 2019, pp.502-511, DOI: 10.1109/ICCV.2019.00059 — 라벨 불신 전제의 모델측 접근 (인용만, 채택 안 함 — §7)
- Agnew et al., IEEE Access 2023 — 학습 GT 노이즈 주입 방법론 전례 / NBBOX 2024 — 박스 섭동이 성능을 올리기도 함(가설 양방향)

### 10.3 포지셔닝 문장 (초안)

"학습 데이터의 혼합 비율로부터 성능을 예측하는 공식화는 대규모 언어모델 사전학습에서 활발히 연구되고 있으나(Data Mixing Laws, DoReMi, RegMix), 객체 탐지에서는 배경 이미지 비율에 대한 실험 근거 없는 경험칙(Ultralytics 0~10%)이 통용되는 실정이다. 본 연구는 총량을 고정한 simplex-centroid 혼합물설계를 화재·연기 탐지 데이터 구성에 적용하여, 화염·연기·정상배경 비율로부터 탐지 성능을 예측하는 공식을 적합하고 전향적으로 검증한 최초의 연구이다."

---

## 11. 예상 반문과 답 (교수님 보고 대비)

**"Scheffé도 결국 남이 만든 이론 아닌가? 우리만의 공식이 어디 있나?"**
→ 회귀분석 자체는 표준 도구지만 그것으로 적합한 스케일링 법칙이 Chinchilla의 기여로 인정되듯, Scheffé는 적합 **도구**이고 본 연구의 산출물은 **적합된 계수(화재 탐지 데이터 구성 법칙)와 그것의 전향적 예측 검증**이다. "혼합물설계를 객체 탐지 데이터 구성에 적용"한 직접 선행이 없음은 문헌 조사로 확인(§10.1).

**"이거 결국 C1~C4를 6,000장으로 다시 돌리는 것 아닌가?"**
→ 세 가지 질적 차이: ① 기존은 셀마다 총량이 달라(3,828~114,462) 총량과 구성이 교락 — 이번엔 전 셀 N 완전 고정, ② 기존은 동시라벨 포함으로 성분 정의가 불순 — 이번엔 순수 단일라벨 풀, ③ 기존은 관찰(순서 보고)에 그침 — 이번엔 공식 적합 + 사전등록 예측 검증. 기존 C1~C4는 "왜 정밀 설계가 필요한가"의 예비 실험으로 재배치.

**"비율을 겨우 3단계(0, ½, 1)로 바꿔서 곡선이 나오나?"**
→ 7점은 special cubic 적합의 최소 완전 설계이고, V1~V3 축점(1/6 저비율 포함)이 내삽 검증을 담당한다. 저비율 급포화(기존 C2 실측이 시사)가 확인되면 Phase B에서 simplex-lattice {3,2}로 변당 점을 추가하며, **기존 C2와 연결되는 14:1(x_SM≈1/15) 레거시 점을 Phase B 검증점 후보**로 두어 기존 관찰과 새 공식을 잇는다.

**"특정 데이터셋에서만 보이는 패턴이 무슨 기여인가? 데이터셋 바꾸면 그만 아닌가?"**
→ 정확한 지적이고, 그래서 논문의 주장은 계수가 아니라 **구조의 불변성**이다(§5). 각 데이터셋의 공식은 측정 도구이고, 화재 3셋 + 비화재 1셋에 같은 도구를 대어 "부호·우세 순위·임계 거동 중 무엇이 도메인을 넘는가"를 분리하는 것이 일반적 지식이다. 예비 증거로 구성 순서·NM 방향·용량 포화가 이미 두 데이터셋에서 교차 재현되어 있다(§5.3). 불변이 확인되면 일반 법칙, 깨지면 도메인 특이성의 정량화 — 어느 쪽이든 "감으로 정하던 데이터 구성"에 근거를 준다.

**"라벨 품질(GIGO) 얘기는 어디 갔나?"**
→ 두 갈래로 병행: ① 평가측 박스 섭동(±10/20%) 민감도 — 기존 C4 체크포인트로 재채점만 하면 되어 파일럿과 병행 실측 중, ② AIHub 라벨 무결성 자동 감사(퇴화 박스·경계 밖·중복 카운트) — "이상 라벨 X%" 실측치 확보. 라벨 불확실성의 모델측 접근(Gaussian YOLOv3)은 인용만 하고 채택하지 않음 — 탐지기를 바꾸면 구성 효과와 재교락되기 때문(§7).

## 12. 미결정 사항 (승인 시 함께 결정 필요)

1. **N 확정**: 28,374 제안 — 분석세션 재카운트 후 확정
2. **P3(정상 단독) 포함 여부**: 포함 제안 (앵커 역할, 저비용) — 제외 시 모델을 6모수로 축소
3. **검증점 개수**: 3점 제안 — 더 엄격하게 하려면 내부점 추가
4. **D-Fire 재현 시점**: AIHub 적합 성공 확인 후 착수 제안 (실패 시 설계 수정 먼저)
5. **NM 포화 곡선(§6)·학습측 라벨 섭동(§7)**: 선택 항목 — 일정 여유 시
6. **비화재 대조 데이터셋 선정 (v1.3)**: 2클래스+배경 구조로 재구성 가능한 공개 탐지 셋 — 후보: COCO 서브셋(예: person·car + 두 클래스 무포함 배경), VOC 파생, 또는 드론·보행자 등 도메인 공개 셋. 선정 기준: ① 클래스별 순수 단일라벨 풀이 수천 장 이상, ② 배경(무라벨) 이미지 확보 가능, ③ 전처리 비용 낮음
